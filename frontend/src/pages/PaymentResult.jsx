import { Link, useSearchParams } from 'react-router-dom'
export default function PaymentResult() { const [params] = useSearchParams(); const status = params.get('status') || 'pending'; const reference = params.get('reference'); return <main className="min-h-[70vh] bg-[#f8f3ef] px-4 py-16 text-center"><section className="mx-auto max-w-2xl rounded-[2rem] bg-white p-10"><p className="eyebrow">Resultado del pago</p><h1 className="mt-4 font-serif text-4xl">Estado: {status}</h1><p className="mt-4 text-[#705d65]">El backend es la fuente de verdad; esta pantalla no marca pedidos como pagados.</p>{reference && <Link className="button-primary mt-7" to={`/orders/${reference}`}>Consultar pedido</Link>}</section></main> }
